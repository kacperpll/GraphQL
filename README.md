# graphql_http_books_nuxt_vuex

> GraphQL Nuxt Vuex http_books

## Build Setup

``` bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run dev

# launch server
$ node server
